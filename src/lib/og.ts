/**
 * Datos de la imagen Open Graph de la raíz, compartidos por `app/opengraph-image`
 * y por los metadatos de las páginas internas (lib/metadatos.ts), que la declaran
 * de forma explícita sin importar el módulo que la dibuja.
 */
export const ogAlt = "Novum Integral — Software y consultoría para el sector solidario";
export const ogSize = { width: 1200, height: 630 };
